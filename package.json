
{
  "name": "chatlocal",
  "version": "1.0.0",
  "description": "",
  
  "scripts": {
    
    "build": "cd frontend && npm ci --include=dev && npm run build && npm audit fix && ls -la dist ",
    "start" : "cd backend && npm install && npm audit fix && ",

    "dev_fe": "cd frontend && npm run dev -- --host",
    "dev_be": "node --watch backend/src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}